def foo(int a, int b) : int {
  int x;
  int y;
  int z;

  x:=1;
  y:=a;

  while(![x=b]) {
     z:=x*2;
     if(z<=b) {
       y:= y*y;
       x:= x*2;
      }
     else {
       y:= y*a;
       x:= x+1;
      }
     }

    return y;
  }
output 4;
